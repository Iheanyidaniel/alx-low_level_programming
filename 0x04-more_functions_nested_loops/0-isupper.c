#include "main.h"
/**
* _isupper - Checks if letters are uppercase.
* @c: char to check
* Return: 1 if uppercase, otherwise 0
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
