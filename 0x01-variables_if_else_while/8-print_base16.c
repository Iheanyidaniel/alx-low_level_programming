#include <stdio.h>
/**
* main - prints all numbers of base 16
* Return: Always 0
*/
int main(void)
{
int b
for (b = 48; b < 58; b++)
{
putchar(b);
}
for (b = 97; b < 103; b++)
{
putchar(i);
}
putchar('\n');
return (0);
}

